# The INTO/ drop is a client handover pack. The brief itself notes the Windsor API key was shared
# in plain text in those connector URLs and must be rotated into Secret Manager: so this folder
# is treated as credential-bearing and never committed.
INTO/

# Locally built demo payloads (regenerated by job/build_local.py)
data/
